# SMART Log Plotter

A small React app for parsing SMART log files and plotting attributes over time.

## Quick start

```bash
npm install
npm run dev
```

Then open the dev server URL shown in the terminal.
